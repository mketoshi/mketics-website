# Step 78 — Client Service Request Centre

1. Replace the included files while preserving their folder structure.
2. Run `supabase/client-service-request-centre.sql` in Supabase SQL Editor.
3. Deploy the website.
4. Test Client Portal → Service Requests, then Admin → Service Requests.

The migration is self-contained and safe to rerun. Attachments are private, limited to 10 MB and restricted to the linked client or authorised MKETICS staff.
